(function() {
  var db = angular.module(["database"], "calculator");
  
  db.service("planningCalc", [
    "$q", "$timeout", "planningDB",
    function($q, $timeout, planningDB) {
      
    }
  ]);
  
  
})()


